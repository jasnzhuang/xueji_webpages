@{
    Layout = "~/_xuejiLayout.cshtml";
    //这里是和JS差不多的一个偷懒变量定义方法，用var来定义
    var majors_name = "";


    //这里吧，是说咱们在页面上填写的信息，怎么特么的就跑到程序里面来了呢？
    majors_name = Request.Form["majors_name"];


    if (IsPost)
    {
        //这句吧，就是打开这个SQLCE数据库，这个SQLCE用法跟SQL没啥太大区别，就是不用用户名密码和IP了
        var db = Database.Open("StarterSite");
        //这里写清楚了所谓的顺序
        var insertCommand = "INSERT INTO majors (majors_name) Values(@0)";
        //这句就是写如何插入到数据库，并且告诉程序要插入的数据都从哪里来的，也就是都从哪几个变量来了
        db.Execute(insertCommand, majors_name);
    }
}
<ul class="breadcrumb">
    <li>
        <i class="icon-home"></i>
        <a href="~/index.cshtml">控制面板</a>
        <i class="icon-angle-right"></i>
    </li>
    <li><a href="#">添加专业</a></li>
</ul>
<form method="post">
    <fieldset>
        <legend>专业信息</legend>
        <p>
            <label for="majors_name">专业名称：</label>
            <input id="majors_name" type="text" name="majors_name" value=""/>
        </p>


        <p>
            <input type="submit" name="buttonSubmit" value="添加专业"/>
        </p>

    </fieldset>
</form>
<p>
    <a href="~/majors_list"></a>
</p>
